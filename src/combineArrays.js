/**
 * Takes in two arrays and combines them into one array
 * with the first arrays being first then the second arrays values being
 * second
 *
 * @param {any[]} oneArr
 * @param {any[]} twoArr
 * @returns the combined arrays
 *
 * @remarks
 *
 * Arrays will always be arrays and wont be undefined or null
 *
 * Arrays will not always have an element.
 */
module.exports = function (oneArr, twoArr) {
  // code here
};
